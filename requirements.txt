# Core data & math
numpy>=1.24,<2.0
pandas>=2.1,<3.0
pyarrow>=14.0,<15.0

# Modeling & ML
scikit-learn>=1.3,<1.6
imbalanced-learn>=0.11,<0.13
joblib>=1.3,<2.0

# Visualization
matplotlib>=3.8,<3.9
seaborn>=0.13,<0.14

# Interpretability (opcional, comentar si no se usa)
shap>=0.44,<0.46

# Notas:
# - Versiones acotadas para estabilidad (rangos < próxima major).
# - Si aparece conflicto, puedes relajar quitando la parte <X.Y.
# - Ejecuta: pip install -r requirements.txt
